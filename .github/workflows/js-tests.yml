# .github/workflows/js-tests.yml
name: JS Tests

on:
  workflow_call:

permissions:
  contents: read

jobs:
  workspaces:
    name: List npm workspaces
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      packages: ${{ steps.set-matrix.outputs.packages }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - uses: ./.github/actions/retry
        with:
          command: npm exec --package=npm@11.18.0 --ignore-scripts -- npm ci --ignore-scripts --min-release-age=7
      - id: set-matrix
        run: |
          PACKAGES=$(npm query .workspace | jq -c '[.[].location]')
          if [ "$PACKAGES" = "[]" ] || [ -z "$PACKAGES" ]; then
            echo "::error::Workspace discovery produced an empty package list — refusing to emit a zero-length matrix (would skip all JS/TS checks silently)."
            exit 1
          fi
          echo "packages=$PACKAGES" >> "$GITHUB_OUTPUT"

  check:
    name: Typecheck & Test
    needs: workspaces
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      matrix:
        package: ${{ fromJson(needs.workspaces.outputs.packages) }}
      fail-fast: false # report all failures, not just the first one
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
      - uses: ./.github/actions/retry
        with:
          command: npm exec --package=npm@11.18.0 --ignore-scripts -- npm ci --ignore-scripts=false --min-release-age=7
      - env:
          PACKAGE: ${{ matrix.package }}
        run: npm run --prefix "$PACKAGE" check
      - env:
          PACKAGE: ${{ matrix.package }}
        run: npm run --prefix "$PACKAGE" fix
