# Supply-chain cooldown: npm 11.10+ ignores package versions published in
# the last seven days when resolving or installing from lockfiles. This blocks
# newly-published malicious versions during the usual 24-72h takedown window.
# CI pins npm 11.x before npm installs so this setting is active there; older
# local npm versions may parse but not enforce it.
min-release-age=7
